name: Feature Workflow
on:
  pull_request:
    paths:
      - '**'
  push:
    branches:
      - 'feature/**'

permissions:
  contents: 'read'
  id-token: 'write'

env:
  IMAGE_URL: ${{ vars.GCP_DEV_IMAGE_REGISTRY_HOST }}/${{ vars.GCP_DEV_PROJECT_ID }}/${{ vars.GCP_DEV_IMAGE_REGISTRY_NAME }}/${{ vars.PIPELINE_NAME }}
  IMAGE_TAG: ${{ github.sha }}

jobs:
#  build:
#    name: Build and Push
#    runs-on: ubuntu-latest
#
#    steps:
#      - name: Checkout
#        uses: actions/checkout@v4
#
#      - name: Authenticate to Google Cloud
#        id: auth
#        uses: google-github-actions/auth@v2
#        with:
#          workload_identity_provider: ${{ secrets.GCP_DEV_WORKLOAD_IDENTITY_PROVIDER }}
#          service_account: ${{ secrets.GCP_DEV_SERVICE_ACCOUNT }}
#
#      - name: Set up Docker
#        uses: docker/setup-buildx-action@v3
#
#      - name: Configure Docker to use gcloud as a credential helper
#        run: gcloud auth configure-docker ${{ vars.GCP_DEV_IMAGE_REGISTRY_HOST }}
#
#      - name: Build Image
#        run: |
#          docker build -t ${{ env.IMAGE_URL }}:${{ env.IMAGE_TAG }} .
#
#      - name: Run Trivy vulnerability scan
#        uses: aquasecurity/trivy-action@master
#        with:
#          image-ref: ${{ env.IMAGE_URL }}:${{ env.IMAGE_TAG }}
#          format: 'table'
#          severity: 'CRITICAL,HIGH'
#
#      - name: Push Image
#        run: docker push ${{ env.IMAGE_URL }}:${{ env.IMAGE_TAG }}


  deploy:
    name: Trigger Deployment Workflow
#    needs:
#      - build
    uses: ./.github/actions/dispatch_deployment_workflow
    with:
      pipeline_name: ${{ vars.PIPELINE_NAME }}
      branch_name: ${{ github.ref }}
      deployment_workflow_repo: ${{ vars.DEPLOYMENT_WORKFLOW_REPO }}
      deployment_workflow_name: ${{ vars.DEPLOYMENT_WORKFLOW_NAME }}
      image_url: 'BOB'
    secrets:
      bearer_token: ${{ secrets.PAT }}
